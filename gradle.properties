org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Nexora Cloud backend. Swap this single line when moving to your own domain.
# (The API token stays in the NEXORA_API_TOKEN GitHub secret — never in this repo.)
nexoraApiUrl=https://solene-64311f39.base44.app
